name: Test Suite

on:
  push:
    branches: [ main, staging, develop ]
  pull_request:
    branches: [ main, staging, develop ]

permissions:
  contents: read

jobs:
  test:
    name: Go Unit & Integration Tests
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version-file: NexusAI-Gateway/go.mod
          cache-dependency-path: NexusAI-Gateway/go.sum
          cache: true

      - name: Install Dependencies
        run: |
          cd NexusAI-Gateway
          go mod download

      - name: Run Tests
        run: |
          cd NexusAI-Gateway
          go test -v -race -coverprofile=coverage.out ./...

      - name: Upload Coverage Report
        uses: actions/upload-artifact@v4
        with:
          name: go-coverage
          path: NexusAI-Gateway/coverage.out
